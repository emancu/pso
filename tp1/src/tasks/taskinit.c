#include <syscalls.h>
#include <tipos.h>
#include <fs.h>
#include <i386.h>
// #include <lib_str.h>

int idx;

int main(void) {
	//	char command[14] = "run task1.pso";
	//	run(command);
//	int i = 0;
//	char* ps1 = "initinttttttt";
//	int fd = open("/console", 0x3);
	while (1) {
//		if (i++ % 100000000 == 0)
//			write(fd, ps1, 14);
	}
	//	run(command);
	// void* p = palloc();
	return 0;
}
